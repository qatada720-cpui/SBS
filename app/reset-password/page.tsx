import { ResetPasswordPage } from '@/components/pages/reset-password';

export const metadata = { title: 'Reset password — SafeBusinessSelling' };

export default function Page() {
  return <ResetPasswordPage />;
}
