import { HowCalculatedPage } from '@/components/pages/how-calculated';

export const metadata = { title: 'How is the score calculated? — SafeBusinessSelling' };

export default function Page() {
  return <HowCalculatedPage />;
}
